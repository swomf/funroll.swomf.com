# corsair

I use app-misc/ckb, an unofficial software to add
functionality to Corsair-brand peripherals (such as side buttons
on mice). Thus I add qt6 support to its dependency:

```bash path=/etc/portage/package.use/peripherals
dev-libs/quazip qt6
```
